# Codexの作業ルール

## プロジェクトと役割

- 購入申請・承認・発注から、備品の貸出・返却・廃棄までを管理する業務アプリを開発する。
- ユーザーは要件、優先順位、仕様の判断、レビュー、承認を担当する。
- Codexは調査、変更案の作成、実装、必要なテスト、差分確認、結果の説明を担当する。
- 説明は日本語で行う。専門用語は必要に応じて意味を添え、初心者にも判断できるようにする。

## Gitでの作業

- [開発手順書](docs/development-workflow.md)に従う。承認のタイミングとGit操作の詳細は同文書を参照する。
- 作業開始時に作業ディレクトリ、ブランチ、未コミットの変更を確認する。リモート操作の前には接続先も確認する。
- 通常の変更は`feature/<作業内容>`ブランチで行い、`main`へ直接コミット・プッシュしない。
- ユーザーの既存の変更を勝手に削除、上書き、取り消ししない。依頼と無関係な変更をコミットに含めない。
- 承認済みの範囲は繰り返し承認を求めず進める。判断が必要な場合は、具体的な変更案と判断理由を示す。

## 実装と確認

- トークン消費を抑えるため、Computer Use（画面の取得・GUI操作）は原則使用しない。ファイル・CLI・APIによる調査と検証を優先する。
- Computer Useが不可欠な場合は、他の手段では目標を達成できない理由、対象のアプリと操作範囲を事前に説明し、ユーザーの明示的な承認を得てから使用する。開発環境の確認でも同じルールを適用する。
- GUIでの確認が残る場合は、CLIで確認済みの項目と未確認の項目を区別して報告する。

- 依頼の目的に必要な範囲で変更する。使用言語や主要なフレームワークなど、未決定の技術選定は提案して合意を得る。
- 実行環境はM1 Mac、Colima、Dockerを前提とし、コンテナはARM64対応を基本とする。
- 起動・テストの方法が整備されたら、その手順に従う。既存のコンテナや他プロジェクトの環境に影響する操作は避ける。
- 変更に応じた確認を行う。文書のみの変更では、内容の整合性、参照先、差分を確認し、不要なアプリのテストは追加しない。
- 認証情報、トークン、秘密鍵、実データをコミットしない。
- 完了時は変更内容、確認結果、未確認事項、現在のブランチ、コミット・プッシュ・PRの実施状況を報告する。
